# SyncSpace Server

A minimal Node.js + Express + Socket.IO server scaffold for real-time collaboration.

## Getting started

1. Install dependencies: `npm install`
2. Start the server: `npm start`
3. Open the app at `http://localhost:3000`
